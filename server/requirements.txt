# Core dependencies
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-dotenv==1.0.1
# websockets pinned to >=13.0 to satisfy google-genai (requires >=13.0,<15.0)
# and still meets uvicorn's requirement (websockets>=10.4). Using a range
# lets pip select a compatible wheel for the environment.
websockets>=13.0,<15.0
pydantic==2.6.1
python-multipart==0.0.9

# Google Cloud / Gemini
google-genai==0.2.2  # For Lyria music generation
google-generativeai>=0.8.0  # For Gemini vision (frame analysis)

# Video processing
opencv-python==4.9.0.80
numpy==1.26.4
Pillow==10.2.0
yt-dlp>=2025.10.22  # Must be >=2025.10.22 for YouTube nsig decryption
ffmpeg-python==0.2.0
imageio-ffmpeg>=0.4.9

# Frame comparison
imagehash==4.3.1
scikit-image==0.25.2

# Utilities
aiohttp==3.9.3
# Note: 'asyncio' and 'textwrap' are part of the Python standard library and
# must not be listed as pip-installable packages. Keep them out of requirements.
